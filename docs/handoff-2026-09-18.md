# Handoff: youtube-money, 2026-09-18

## Where things stand
- **The project:** a faceless YouTube maker driven from Discord sessions, using Blotato as the only paid service. The folder is `~/main-projects/youtube-money`, with a public repo at github.com/Pu11en/youtube-money.
- **Nothing is built yet.** Research is done, the flow is designed, and there are no skills in `.agents/skills/` yet.
- ✅ **Everything is on GitHub,** including the Sollo reference write-up and this handoff.

## Starting on a new computer
- **Get the folder:** `git clone git@github.com:Pu11en/youtube-money.git` (or the https link), then open Claude Code inside it. `AGENTS.md` loads on its own through `CLAUDE.md`.
- ⚠️ **Not in the repo, so missing on a new computer:**
  - the sister folders the docs mention (`../blotato-automations` with the Blotato API runner, `../cinco-vid` with the free video assembler). Clone them from Drew's GitHub only if a step needs them.
  - full copies of prompts without an open license (`research/style-cloner-sources/prompts/private/`). Each one is linked with its author in `github.md`, `n8n.md` and `community.md`.
  - the full Sollo video transcripts. The write-up has everything needed from them.
  - Drew's own global instructions, the Blotato API key, and OpenCLI logged into Blotato. Ask Drew before anything needs them; none of the planning needs them.
- **Talking to Drew:** 5 sentences max per reply, plain words, no jargon. End every reply with one multiple-choice question of 4–5 real options, the recommended one first.

## What was done this session
- **Designed our own 15-step flow** (`docs/video-flow.md`): what Drew says at each step, what the session saves, and a "done when" check. The steps are channel profile → competitor outliers → style DNA → ideas → sourced facts → outline → script + grader → gate 1 (script OK + credit limit) → scene plan → 3-scene test → full render → thumbnail → gate 2 (watch + OK) → post → learn.
- **Wrote up the exact Sollo flow** from the transcripts of the Short and its full 12-minute tutorial (`research/sollo-video-exact-flow.md`).
  - ⚠️ **Reference only. Drew decided we are NOT using Sollo.** It's there to learn what a working flow looks like, step by step.
  - Their 10 steps: Claude Project "brain" with the niche → screenshot of the competitor's top videos → 15 ideas ranked by clicks → 10 little-known facts → Sollo script form (title, facts, competitor link as the style, "ad revenue", 10 min) → 50 numbered 2D scenes → voiceover → thumbnail remade from a competitor's → CapCut by hand → "why it flopped" analyzer → bonus PDF product sold on Gumroad.
  - Their exact prompts live on a private Notion page and aren't in the video.
- **Full transcripts stayed on Drew's other computer** (git ignores `research/private-transcripts/`).

## Rules the next session must keep
- **Blotato is the only paid service.** No Kie.ai, HeyGen, Apify, OpenAI keys or Sollo. For a specific video model, drive Blotato's website with OpenCLI.
- **No credits until Drew approves the script and a credit limit. Nothing posts until he has watched it.**
- **Copy structure, never wording** (YouTube's "inauthentic content" rule).
- **Build from the proven prompt kit** (`research/style-cloner-sources/SUMMARY.md`), never invented prompts.
- **Commit locally; push only after Drew's OK.**
- **Big builds get chunked.** Ask Drew "`/gowork` or a normal session?" before writing a build plan.

## Open decisions for the next session
- ⬜ **Should our flow borrow Sollo's two after-posting steps?** A "why it flopped" check from a YouTube Studio screenshot, and a digital-product builder.
- ⬜ **What order to build in?** The recommendation is the free steps first: channel profile, competitor outliers and style DNA, then the writer + grader.
- ⬜ **Which niche goes first** for a no-credit paper run?

## Files to read first
- `AGENTS.md`: the rules and the map
- `docs/video-flow.md`: our 15-step flow
- `research/sollo-video-exact-flow.md`: the Sollo reference
- `research/style-cloner-sources/SUMMARY.md`: the proven prompt kit
